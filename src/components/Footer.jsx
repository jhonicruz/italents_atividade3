import React from 'react';

const Footer = ({ content }) => {
  return (
    <footer className="h-[100px] flex justify-center items-center text-white ">{content}</footer>
  );
};

export default Footer;
